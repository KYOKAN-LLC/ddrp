# Golang CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-go/ for more details
version: 2.1
orbs:
  protobuf: izumin5210/protobuf@0.1.0
jobs:
  build:
    docker:
      # specify the version
      - image: circleci/golang:1.13
        environment:
          GO111MODULE: "on"
    
    steps:
      - protobuf/install:
          version: 3.11.2
      - run: go get github.com/golang/protobuf/protoc-gen-go@1.2.0
      - checkout

      # specify any bash command here prefixed with `run: `
      - run: make ddrpd
      - run: make ddrpcli
      - run: make test
